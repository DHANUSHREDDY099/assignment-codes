// write a code whether the year is leap year or not
#include<stdio.h>
int main()
{
    int a;
    printf("checking whether the year is leap year or not\n");
    printf("enter the year  4");
    scanf("%d",&a);
    if(a%4==0)
     printf("the year is leap year");
    else
     printf("the year is not a leap year");
    
}