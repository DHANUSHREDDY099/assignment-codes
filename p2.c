// size of the function(int,long,float,char)
# include <stdio.h>
int main ()
{
 long a,b,c,d;
 printf("size of the int is %lu bytes\n",sizeof(int) );  
  printf("size of the char is %lu bytes\n",sizeof(char) );
   printf("size of the float is % bytes\n",sizeof(float) );
    printf("size of the long is % bytes\n",sizeof(long) ); 
}